import React, { useEffect } from 'react';
import '../App.css';

const VideoBackground = () => {
  // You can remove useEffect if it's only related to video loading
  return (
    <div className="video-background">
      {/* No video */}
    </div>
  );
};

export default VideoBackground;
